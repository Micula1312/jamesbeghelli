{
  "name": "jamesbeghelli-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open index.html",
    "clean": "rimraf dist",
    "build:js": "esbuild js/app.js --minify --outdir=dist/js",
    "copy:static": "cpx \"index.html\" dist && cpx \".nojekyll\" dist && cpx \"CNAME\" dist && cpx \"*.css\" dist && cpx \"css/**\" dist/css && cpx \"img/**\" dist/img && cpx \"fonts/**\" dist/fonts && cpx \"favicon.*\" dist",
    "build": "npm run clean && npm run build:js && npm run copy:static",
    "preview": "http-server dist -p 8080 -c-1"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "esbuild": "^0.25.9",
    "http-server": "^14.1.1",
    "rimraf": "^5.0.5",
    "vite": "^5.4.8"
  }
}
